const Title = ({ mainTitle }) => {
    return (
        <div>
            <h1 style={{ textAlign: "center", fontWeight: 'bold', borderBottom: '3px solid gray' }}>
                {mainTitle}</h1>
        </div>
    )
}
export default Title